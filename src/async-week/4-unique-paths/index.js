// Recursive, go DOWN, and then go RIGHT


// Your job is to go to the 
// 

const uniquePaths = (m, n, row = 0, col = 0) => {
  if (row === m - 1 && col === n - 1) {
    console.log(m, n)
    return 1
  }
}